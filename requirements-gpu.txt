# Install with:
#   python -m pip install -r requirements-gpu.txt
# The explicit local version prevents pip from selecting the CPU wheel.
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.13.0+cu130
-r requirements.txt
